"""
    Requires node, npm and the ganache cli as brownie has a dependency on it

    npm install -g ganache-cli
    
    brownie bake token -> created the base project
    brownie console -> initialise the console
    dir() -> list of objects
    help(<object-name-from-dir-output>) -> returns docstring for each object


"""